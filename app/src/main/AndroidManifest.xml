<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="qslide.example.lv">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MeetingsActivity" />
        <activity android:name=".Meetings" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Client"></activity>
        <activity android:name=".Server"></activity>
        <activity android:name=".Dag2"></activity>
        <activity android:name=".Dag2NewPodt"></activity>
        <activity android:name=".LiveReportActivity" />
        <activity android:name=".CaughtFishActivity" />
        <activity android:name=".NoFishActivity" />
        <activity android:name=".Dag2Main"></activity>
    </application>

</manifest>
